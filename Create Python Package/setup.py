from setuptools import setup, find_packages

setup(
    name='hackers_realm',
    version='1.0',
    packages=find_packages(),
    install_requires = [
        # add any dependencies
        # 'pandas', 'numpy'
    ]
)